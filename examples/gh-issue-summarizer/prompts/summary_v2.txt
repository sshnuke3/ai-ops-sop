# prompts/summary_v2.txt —— gh-issue-summarizer 的摘要 prompt 历史版本
#
# 状态：已弃用（DEP-N，详见 spec.md §4 演进事件）
# 当前生效版本：prompts/summary_v3.txt
# 弃用日期：2026-07-19
#
# 为什么保留：spec.md §"事件类型 DEP-N" 规范要求——旧版本不删，作为
# "模型/数据兼容性"对照资产。如果未来 LLM 行为变化，可以重跑 v2 看历史数据是否还能复现。

# ============================================================
# v2 vs v3 差异（仅作对照，不再生效）
# ============================================================
#
# v2 的 prompt（已删除的旧版骨架示意）：
#
#   "请把以下 issue 总结成 3 句话中文。"
#   "标题：{{.IssueTitle}}"
#   "正文：{{.IssueBody}}"
#
# v2 没有显式约束"不许 Markdown/不许编造/不许 4 句"等 → 实际跑 20 次里：
#   - 11 次输出 4 句（违反"恰好 3 句"）
#   - 6 次加了 **加粗** 标记（违反 Notion 字段纯文本）
#   - 4 次编造 "该 issue 由 ... 提出"（用户没在 body 写）
#   - 2 次拒绝输出（"我不能处理个人信息"）
#
# v3 修了这 5 类问题 → 升 v3。
# 详细修复过程见 git log `bb93c44` 与 spec.md §4 "BUG-2 fix" 条目。

# === v2 的真正含义：作为负面教材保留 ===
# 【填法】保留的不是"能用的 prompt"，而是"对照 / 反例"。
# 【为什么这样写】spec.md §"事件类型"规定 DEP-N 不删——未来人 / AI 看到 v2 知道"为什么 v3 那样写"。
# 比 git history 里翻 commit diff 直观得多。

# === 本文件示例特意展示的 2 件事 ===
# 1. DEP-N 事件之后，旧版本不删只留为反例（spec.md §"事件类型"实操演示）
# 2. v2 与 v3 差异是"5 类症状 + 数量"——这是 BUG-N 索引里的实战细节
